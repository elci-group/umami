[package]
name = "umami"
version = "0.1.2"
edition = "2024"
description = "Multi-tier memory pressure buffering daemon for Linux"
license = "MIT"
repository = "https://github.com/elci-group/umami"

[dependencies]
